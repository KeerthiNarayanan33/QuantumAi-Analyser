"""
============================================================
QuantumSentinel — Backend Package
============================================================
FastAPI REST API that exposes all core intelligence modules
as HTTP endpoints.

Endpoints are organized into routers:
  /api/stocks      — OHLCV price data
  /api/sentiment   — FinBERT sentiment analysis
  /api/predict     — Buy / Hold / Sell signals
============================================================
"""
